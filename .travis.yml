language: haskell
sudo: false
ghc:
  - '7.8'
env:
  global:
    - SNAPSHOT_VERSION: 'nightly-2015-08-08'
    - GHC_VERSION: '7.10.1'
    - STACK_RELEASE: 'https://github.com/commercialhaskell/stack/releases/download/v0.1.3.0/stack-0.1.1.0-x86_64-linux.gz'

sudo: false
cache:
  directories:
  - $HOME/.stack/snapshots/x86_64-linux/$SNAPSHOT_VERSION/$GHC_VERSION/

before_install:
  - ls $HOME/.stack/snapshots/x86_64-linux/$SNAPSHOT_VERSION/
  - ls $HOME/.stack/snapshots/x86_64-linux/$SNAPSHOT_VERSION/$GHC_VERSION/
  - mkdir -p $HOME/bin
  - curl -L "$STACK_RELEASE" | gzip -d > $HOME/bin/stack
  - chmod +x $HOME/bin/stack

install:
  - ls $HOME/.stack/snapshots/x86_64-linux/$SNAPSHOT_VERSION/
  - ls $HOME/.stack/snapshots/x86_64-linux/$SNAPSHOT_VERSION/$GHC_VERSION/
  - stack setup
  - stack test --only-snapshot

script:
  - stack test
